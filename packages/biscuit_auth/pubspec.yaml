name: biscuit_auth
description: A starting point for Dart libraries or applications.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  collection: ^1.19.1
  meta: ^1.19.0
  petitparser: ^7.0.2

dev_dependencies:
  build_runner: ^2.16.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  kiri_check: ^1.3.1
